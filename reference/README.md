# Java reference trees

Vendored **source** from the upstream L2 Unity servers. Used as the
protocol and service-API spec for the Go rewrite.

- `l2-unity-loginserver` — https://github.com/shnok/l2-unity-loginserver
- `l2-unity-gameserver` — https://github.com/shnok/l2-unity-gameserver

XML datapack / geodata files are not copied (see
`l2-unity-gameserver/gameserver/data/README.md`).
